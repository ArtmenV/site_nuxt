import { $api } from '~/api/api'

export const state = () => ({
  votingList: []
})

export const mutations = {
  votingList: (state, payload) => state.votingList = payload,
}

export const actions = {
  
}


export const getters = {

}
